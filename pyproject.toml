[project]
name = "autoipaalign"
version = "0.1.0"
description = "Automatic IPA alignment workspace with CLI, comparison tools, and web interface"
requires-python = ">=3.9"
readme = "README.md"

[tool.uv.workspace]
members = ["packages/*"]

[tool.ruff]
line-length = 120
